---
title: 'Estrutura Básica'
description: 'Aqui está a estrutura básica do projeto'
icon: 'folder'
---

To denote a `word` or `phrase` as code, enclose it in backticks (\`).

```
To denote a `word` or `phrase` as code, enclose it in backticks (`).
```