---
title: "Introdução"
description: "Introdução a Api Weather desenvolvida com micro serviços."
icon: "book"
---

<Info>
  **Atenção**: Esta API é destinada exclusivamente para fins de teste, como parte de projetos académicos ou de trabalho universitário.
</Info>

## Arquitetura

### Gateaway

Recebe as requisições `HTTP` dos utilizadores e distribui para os microserviços correspondentes.

```bash
ms-gateaway
```

### Weather

Consulta a API externa `(OpenWeather)` para obter informações meteorológicas.

```bash
ms-weather
```

### Geo Service

Usa o `IP` do utilizador para determinar sua localização geográfica.

```bash
ms-geo
```

### Database

Armazena os dados climáticos no base de dados `Supabase`.

```bash
ms-database
```